class Car{

 infix    fun drive( speed:Int){
        println("Drive the car  at ${speed} KM per Hour")
    }
}

// display date in startDate..endDate step 7     (  startDate..endDate step 7  )
// Display is the object /  data is a function  /(  intProgression : IntProgression   )

fun main(){

    val car = Car()
   // car.drive( 80 )


    car drive 100
}

/*

// intProgression
// 1st assignment.. Got it almost..
display { date in startDate..endDate step 7 }


for( i in 1 until 10 ) println(i)


// We can create our own API in kotlin

MyFor( 1 in 1 until 10){  it -> DisplayData( it )  }

MyFor( 1 in 1 until 10)   {  DisplayData( it )  }

MyFor( 1 in 1 until 10   ) DisplayData( it )
class(  constructor      ) fun_DisplayDta( it )      // think..

fun MyFor( range:IntRange () -> Unit){
  // lambda
}

// Expressive language..

// Various conditions has to be met internally
"When User entered credentials"{ // name : pw ( T) Size = 0
    "and clicked on login button" {  // n Y next line
        "and the crediantials were valid, then the app {
                // take me home
                // "When User entered credentials"{
              should("Swich to the main flow"){
            // code goes here    call an activity
        }
    }
 }
}  // out


SELECT column_list FROM table;

SELECT * FROM table_name;
// *   table_name   (INPUTS expected from you )


SELECT tbl_name FROM sqlite_master WHERE type = 'table';

SELECT sql FROM sqlite_master WHERE type = 'table' AND tbl_name = 'COMPANY';

CREATE TABLE COMPANY(
ID INT PRIMARY KEY     NOT NULL,
NAME           TEXT    NOT NULL,
AGE            INT     NOT NULL,
ADDRESS        CHAR(50),
SALARY         REAL
)

Jetpack Compose..






*/